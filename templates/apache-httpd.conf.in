# EDRN configuration
# ==================
#
# Apache HTTPD customizations for Early Detection Research Network


# LabCAS UI
# ---------
#
# Settings for LabCAS
WSGIApplicationGroup %{GLOBAL}
WSGIPythonHome ${paths:python-home}
WSGIPassAuthorization On
WSGIDaemonProcess labcas user=${users:labcas} group=${groups:labcas} threads=4 python-path=${paths:python-home}/lib/python${paths:python-version}/site-packages
WSGIScriptAlias /manage ${buildout:bin-directory}/${buildout:wsgi-target-name}
<Directory ${buildout:bin-directory}>
    WSGIProcessGroup labcas
    Options FollowSymLinks
    <Files ${buildout:wsgi-target-name}>
        Require all granted
    </Files>
</Directory>
